"""Legacy setuptools entry point for psycgod-sage.

Package installation is intentionally passive. Explicit ``sage install`` or a
normal first CLI invocation performs any optional local activation.
"""

from setuptools import setup


setup()
